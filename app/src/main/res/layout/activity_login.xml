<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" >
    <data>
        <variable
            name="activity"
            type="com.mkm.hanium.jjack.login.LoginActivity" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="144dp">

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/btn_login_guest"
            android:layout_width="match_parent"
            android:layout_height="@dimen/btn_height"
            android:text="@string/start_guest"
            android:background="@drawable/selector_button_signup"
            android:textColor="@color/color6"
            android:layout_marginLeft="@dimen/btn_big_margin"
            android:layout_marginRight="@dimen/btn_big_margin"
            android:layout_marginBottom="@dimen/btn_big_margin"
            android:paddingLeft="@dimen/btn_margin"
            android:paddingRight="@dimen/btn_margin"
            android:elevation="@dimen/btn_elevation"
            android:translationZ="0dp"
            android:textSize="@dimen/text_size_btn"
            android:onClick="@{activity::onClick}"/>

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/btn_login_kakao"
            android:layout_width="match_parent"
            android:layout_height="@dimen/btn_height"
            android:text="@string/start_kakao"
            android:background="@drawable/selector_button_kakao"
            android:textColor="@color/kakao_text"
            android:textStyle="bold"
            android:layout_marginLeft="@dimen/btn_big_margin"
            android:layout_marginRight="@dimen/btn_big_margin"
            android:paddingLeft="@dimen/btn_margin"
            android:paddingRight="@dimen/btn_margin"
            android:elevation="@dimen/btn_elevation"
            android:translationZ="0dp"
            android:textSize="@dimen/text_size_btn"
            android:onClick="@{activity::onClick}"/>

    </LinearLayout>
</layout>